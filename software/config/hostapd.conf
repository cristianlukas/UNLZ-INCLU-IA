# Inclu-IA AP config (hostapd)
interface=wlan0
driver=nl80211
ssid=Inclu-IA_Classroom
hw_mode=g
channel=6
country_code=AR
ieee80211n=1
wmm_enabled=1
auth_algs=1
ignore_broadcast_ssid=0

# WPA2-PSK (AES/CCMP only)
wpa=2
wpa_key_mgmt=WPA-PSK
wpa_passphrase=CambiarEstaClave123!
rsn_pairwise=CCMP